<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MagiDesk.Frontend.Views.WalletManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagiDesk.Frontend.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Using Fluent styles from DesignSystem.xaml -->
    </Page.Resources>

    <ScrollViewer VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
        <Grid MaxWidth="1000" HorizontalAlignment="Center" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}" Padding="24,16" CornerRadius="8" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Back Button -->
                    <Button Grid.Column="0" 
                            x:Name="BackButton"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="8"
                            Margin="0,0,16,0"
                            Click="BackButton_Click">
                        <FontIcon Glyph="&#xE72B;" FontSize="16"/>
                    </Button>

                    <!-- Customer Avatar -->
                    <Border Grid.Column="1" 
                            Width="48" 
                            Height="48" 
                            CornerRadius="24" 
                            Background="{ThemeResource AccentFillColorDefaultBrush}"
                            Margin="0,0,16,0">
                        <TextBlock x:Name="CustomerInitialsText" 
                                   Text="--" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="White" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock x:Name="CustomerNameText" 
                                   Text="Loading..." 
                                   FontSize="20" 
                                   FontWeight="Bold" 
                                   Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        <TextBlock Text="Wallet Management" 
                                   FontSize="14" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <ProgressRing x:Name="LoadingRing" 
                                  Grid.Column="3"
                                  Width="24" 
                                  Height="24" 
                                  IsActive="False"/>
                </Grid>
            </Border>

            <!-- Wallet Overview -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Current Balance -->
                <Border Grid.Column="0" Style="{StaticResource SectionCardStyle}" Background="{ThemeResource AccentFillColorDefaultBrush}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="WalletBalanceText" 
                                   Text="$0.00" 
                                   FontSize="32" 
                                   FontWeight="Bold" 
                                   Foreground="White"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Current Balance" 
                                   FontSize="14" 
                                   Foreground="White" 
                                   Opacity="0.9"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Total Loaded -->
                <Border Grid.Column="2" Style="{StaticResource SectionCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="TotalLoadedText" 
                                   Text="$0.00" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Loaded" 
                                   FontSize="14" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Total Spent -->
                <Border Grid.Column="4" Style="{StaticResource SectionCardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="TotalSpentText" 
                                   Text="$0.00" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Spent" 
                                   FontSize="14" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column - Actions -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <!-- Add Funds -->
                    <Border Style="{StaticResource SectionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Add Funds" 
                                       FontSize="18" 
                                       FontWeight="SemiBold" 
                                       Margin="0,0,0,16"/>

                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Amount" 
                                           FontSize="14" 
                                           FontWeight="SemiBold" 
                                           Margin="0,0,0,8"/>
                                <TextBox x:Name="AddFundsAmountTextBox" 
                                         PlaceholderText="Enter amount to add"
                                         InputScope="Number"
                                         CornerRadius="6"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Description" 
                                           FontSize="14" 
                                           FontWeight="SemiBold" 
                                           Margin="0,0,0,8"/>
                                <TextBox x:Name="AddFundsDescriptionTextBox" 
                                         PlaceholderText="Optional description"
                                         CornerRadius="6"/>
                            </StackPanel>

                            <Button x:Name="AddFundsButton" 
                                    Content="Add Funds" 
                                    Style="{StaticResource ActionButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    Click="AddFundsButton_Click"/>
                        </StackPanel>
                    </Border>

                    <!-- Deduct Funds -->
                    <Border Style="{StaticResource SectionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Deduct Funds" 
                                       FontSize="18" 
                                       FontWeight="SemiBold" 
                                       Margin="0,0,0,16"/>

                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Amount" 
                                           FontSize="14" 
                                           FontWeight="SemiBold" 
                                           Margin="0,0,0,8"/>
                                <TextBox x:Name="DeductFundsAmountTextBox" 
                                         PlaceholderText="Enter amount to deduct"
                                         InputScope="Number"
                                         CornerRadius="6"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="Description" 
                                           FontSize="14" 
                                           FontWeight="SemiBold" 
                                           Margin="0,0,0,8"/>
                                <TextBox x:Name="DeductFundsDescriptionTextBox" 
                                         PlaceholderText="Optional description"
                                         CornerRadius="6"/>
                            </StackPanel>

                            <Button x:Name="DeductFundsButton" 
                                    Content="Deduct Funds" 
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    Click="DeductFundsButton_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Right Column - Transaction History -->
                <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                       Text="Transaction History" 
                                       FontSize="18" 
                                       FontWeight="SemiBold"/>
                            
                            <Button Grid.Column="1" 
                                    x:Name="RefreshButton"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="8"
                                    Click="RefreshButton_Click">
                                <FontIcon Glyph="&#xE149;" FontSize="16"/>
                            </Button>
                        </Grid>

                        <!-- Filter Options -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ComboBox Grid.Column="0" 
                                      x:Name="TransactionTypeComboBox"
                                      PlaceholderText="All Transactions"
                                      HorizontalAlignment="Stretch"
                                      SelectionChanged="TransactionTypeComboBox_SelectionChanged">
                                <ComboBoxItem Content="All Transactions" Tag=""/>
                                <ComboBoxItem Content="Credits Only" Tag="credit"/>
                                <ComboBoxItem Content="Debits Only" Tag="debit"/>
                            </ComboBox>

                            <ComboBox Grid.Column="2" 
                                      x:Name="DateRangeComboBox"
                                      PlaceholderText="All Time"
                                      HorizontalAlignment="Stretch"
                                      SelectionChanged="DateRangeComboBox_SelectionChanged">
                                <ComboBoxItem Content="All Time" Tag="all"/>
                                <ComboBoxItem Content="Last 7 Days" Tag="7"/>
                                <ComboBoxItem Content="Last 30 Days" Tag="30"/>
                                <ComboBoxItem Content="Last 90 Days" Tag="90"/>
                            </ComboBox>
                        </Grid>

                        <!-- Transaction List -->
                        <ListView x:Name="TransactionsListView" 
                                  MaxHeight="500"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:WalletTransactionItemViewModel">
                                    <Grid Padding="0,12" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Transaction Type Icon -->
                                        <Border Grid.Column="0" 
                                                Width="40" 
                                                Height="40" 
                                                CornerRadius="20" 
                                                Background="{x:Bind TypeColor}"
                                                Margin="0,0,16,0">
                                            <FontIcon Glyph="{x:Bind TypeIcon}" 
                                                      FontSize="16" 
                                                      Foreground="White"/>
                                        </Border>

                                        <!-- Transaction Details -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind Description}" 
                                                       FontWeight="SemiBold"
                                                       FontSize="14"/>
                                            <TextBlock Text="{x:Bind DateText}" 
                                                       FontSize="12" 
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>

                                        <!-- Amount -->
                                        <StackPanel Grid.Column="2" 
                                                    HorizontalAlignment="Right" 
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,16,0">
                                            <TextBlock Text="{x:Bind AmountText}" 
                                                       FontWeight="Bold" 
                                                       Foreground="{x:Bind AmountColor}"
                                                       HorizontalAlignment="Right"
                                                       FontSize="16"/>
                                        </StackPanel>

                                        <!-- Actions -->
                                        <Button Grid.Column="3" 
                                                Background="Transparent" 
                                                BorderThickness="0" 
                                                Padding="8"
                                                VerticalAlignment="Center">
                                            <FontIcon Glyph="&#xE712;" FontSize="14"/>
                                            <Button.Flyout>
                                                <MenuFlyout>
                                                    <MenuFlyoutItem Text="View Details" 
                                                                    Icon="View" 
                                                                    Click="ViewTransactionDetails_Click"/>
                                                </MenuFlyout>
                                            </Button.Flyout>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <!-- No Transactions Message -->
                        <TextBlock x:Name="NoTransactionsText" 
                                   Text="No transactions found" 
                                   HorizontalAlignment="Center" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Visibility="Collapsed"
                                   Margin="0,40,0,0"/>

                        <!-- Pagination -->
                        <Grid Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="PreviousPageButton" 
                                    Grid.Column="0"
                                    Content="Previous" 
                                    IsEnabled="False"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Click="PreviousPageButton_Click"/>

                            <TextBlock x:Name="PageInfoText" 
                                       Grid.Column="1"
                                       Text="Page 1 of 1" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       FontSize="14"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                            <Button x:Name="NextPageButton" 
                                    Grid.Column="2"
                                    Content="Next" 
                                    IsEnabled="False"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Click="NextPageButton_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
