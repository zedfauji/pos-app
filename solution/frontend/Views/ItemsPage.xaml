<Page
    x:Class="MagiDesk.Frontend.Views.ItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ct="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock x:Name="Title" Text="Items" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock x:Name="VendorNameText" VerticalAlignment="Center"/>
        </StackPanel>

        <InfoBar x:Name="FeedbackBar" Grid.Row="1" IsOpen="False" Severity="Informational" Margin="0,8,0,8"/>

        <ct:DataGrid Grid.Row="2"
                     x:Name="ItemsGrid"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     ItemsSource="{Binding Items}">
            <ct:DataGrid.Columns>
                <ct:DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="2*"/>
                <ct:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="3*"/>
                <ct:DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="*"/>
                <ct:DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="*"/>
            </ct:DataGrid.Columns>
            <ct:DataGrid.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem x:Name="CtxEdit" Text="Edit" Click="Item_Edit_Click"/>
                    <MenuFlyoutItem x:Name="CtxDelete" Text="Delete" Click="Item_Delete_Click"/>
                </MenuFlyout>
            </ct:DataGrid.ContextFlyout>
        </ct:DataGrid>
    </Grid>
</Page>
