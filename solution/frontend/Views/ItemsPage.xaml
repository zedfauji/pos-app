<Page
    x:Class="MagiDesk.Frontend.Views.ItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock x:Name="Title" Text="Items" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock x:Name="VendorNameText" VerticalAlignment="Center"/>
        </StackPanel>

        <InfoBar x:Name="FeedbackBar" Grid.Row="1" IsOpen="False" Severity="Informational" Margin="0,8,0,8"/>

        <!-- CRITICAL FIX: Replace CommunityToolkit DataGrid with native WinUI 3 ListView -->
        <!-- CommunityToolkit DataGrid causes COM exceptions in WinUI 3 Desktop Apps -->
        <ListView Grid.Row="2"
                  x:Name="ItemsGrid"
                  ItemsSource="{Binding Items}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- SKU -->
                        <TextBlock Grid.Column="0" Text="{Binding Sku}" VerticalAlignment="Center"/>
                        
                        <!-- Name -->
                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"/>
                        
                        <!-- Price -->
                        <TextBlock Grid.Column="2" Text="{Binding Price}" VerticalAlignment="Center"/>
                        
                        <!-- Stock -->
                        <TextBlock Grid.Column="3" Text="{Binding Stock}" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem x:Name="CtxEdit" Text="Edit" Click="Item_Edit_Click"/>
                    <MenuFlyoutItem x:Name="CtxDelete" Text="Delete" Click="Item_Delete_Click"/>
                </MenuFlyout>
            </ListView.ContextFlyout>
        </ListView>
    </Grid>
</Page>
