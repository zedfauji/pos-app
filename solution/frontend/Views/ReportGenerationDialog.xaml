<ContentDialog
    x:Class="MagiDesk.Frontend.Views.ReportGenerationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Generate Custom Report"
    PrimaryButtonText="Generate"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid MinWidth="500" MinHeight="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Report Type -->
        <TextBlock Grid.Row="0" Text="Report Type" Style="{StaticResource SubtitleTextStyle}" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="1" x:Name="ReportTypeComboBox" PlaceholderText="Select report type" Margin="0,0,0,8">
            <ComboBoxItem Content="Inventory Summary" Tag="InventorySummary"/>
            <ComboBoxItem Content="Vendor Performance" Tag="VendorPerformance"/>
            <ComboBoxItem Content="Transaction Analysis" Tag="TransactionAnalysis"/>
            <ComboBoxItem Content="System Health" Tag="SystemHealth"/>
        </ComboBox>

        <!-- Date Range -->
        <TextBlock Grid.Row="2" Text="Date Range" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
            <DatePicker x:Name="StartDatePicker"/>
            <DatePicker x:Name="EndDatePicker"/>
        </StackPanel>

        <!-- Filters -->
        <TextBlock Grid.Row="4" Text="Filters" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="5" Orientation="Vertical" Spacing="8" Margin="0,0,0,8">
            <CheckBox x:Name="IncludeInventoryCheckBox" Content="Include Inventory Transactions" IsChecked="True"/>
            <CheckBox x:Name="IncludeVendorOrdersCheckBox" Content="Include Vendor Orders" IsChecked="True"/>
            <CheckBox x:Name="IncludeSystemEventsCheckBox" Content="Include System Events" IsChecked="False"/>
            <CheckBox x:Name="IncludeErrorsCheckBox" Content="Include Errors Only" IsChecked="False"/>
        </StackPanel>

        <!-- Additional Options -->
        <TextBlock Grid.Row="6" Text="Additional Options" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="7" Orientation="Vertical" Spacing="8">
            <CheckBox x:Name="ExportToCSVCheckBox" Content="Export to CSV after generation" IsChecked="True"/>
            <CheckBox x:Name="IncludeChartsCheckBox" Content="Include charts and graphs" IsChecked="False"/>
            <CheckBox x:Name="DetailedLogsCheckBox" Content="Include detailed transaction logs" IsChecked="False"/>
        </StackPanel>
    </Grid>
</ContentDialog>
