<Page
    x:Class="MagiDesk.Frontend.Views.TablesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:MagiDesk.Frontend.Views"
    xmlns:conv="using:MagiDesk.Frontend.Converters"
    mc:Ignorable="d">
    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </Page.Resources>
    <ScrollViewer>
        <StackPanel Padding="24" Spacing="24">
            <!-- Legend -->
            <StackPanel Orientation="Horizontal" Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <Ellipse Width="14" Height="14" Fill="#1faa59"/>
                    <TextBlock Text="Available"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <Ellipse Width="14" Height="14" Fill="#c62828"/>
                    <TextBlock Text="Occupied"/>
                </StackPanel>
            </StackPanel>

            <!-- Controls: Filter + Live updates -->
            <StackPanel Orientation="Horizontal" Spacing="16">
                <StackPanel>
                    <TextBlock Text="Filter"/>
                    <ComboBox x:Name="FilterCombo" Width="180" SelectionChanged="FilterCombo_SelectionChanged">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Available"/>
                        <ComboBoxItem Content="Occupied"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Live updates"/>
                    <ToggleSwitch x:Name="LiveToggle" Toggled="LiveToggle_Toggled"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Source"/>
                    <TextBlock x:Name="SourceText"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Counts"/>
                    <TextBlock x:Name="CountsText"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Text="Billiard Tables" FontSize="20" FontWeight="SemiBold"/>
            <GridView ItemsSource="{Binding FilteredBilliardTables}" IsItemClickEnabled="False" SelectionMode="None">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" ItemWidth="220" ItemHeight="150"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="Margin" Value="8"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="200" Height="150" Spacing="6">
                            <StackPanel.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Start" Click="StartMenu_Click" Visibility="{Binding IsAvailable, Converter={StaticResource BoolToVisibility}}"/>
                                    <MenuFlyoutItem Text="Add Items" Click="AddItemsMenu_Click" Visibility="{Binding Occupied, Converter={StaticResource BoolToVisibility}}"/>
                                    <MenuFlyoutItem Text="Stop" Click="StopMenu_Click" Visibility="{Binding Occupied, Converter={StaticResource BoolToVisibility}}"/>
                                </MenuFlyout>
                            </StackPanel.ContextFlyout>
                            <Grid Width="200" Height="110">
                                <Border Background="{Binding BackgroundBrush}" CornerRadius="8" BorderBrush="#0a2a16" BorderThickness="4"/>
                                <!-- pockets -->
                                <Ellipse Fill="#0a2a16" Width="12" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="2"/>
                                <Ellipse Fill="#0a2a16" Width="12" Height="12" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="2"/>
                                <Ellipse Fill="#0a2a16" Width="12" Height="12" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="2"/>
                                <Ellipse Fill="#0a2a16" Width="12" Height="12" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="2"/>
                                <Ellipse Fill="#0a2a16" Width="12" Height="12" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                                <Ellipse Fill="#0a2a16" Width="12" Height="12" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                            </Grid>
                            <TextBlock Text="{Binding Label}" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding StatusText}" HorizontalAlignment="Center" Opacity="0.8"/>
                            <TextBlock Text="{Binding ItemsSummary}" HorizontalAlignment="Center" Opacity="0.8" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <TextBlock Text="Bar Tables" FontSize="20" FontWeight="SemiBold"/>
            <GridView ItemsSource="{Binding FilteredBarTables}" IsItemClickEnabled="False" SelectionMode="None">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" ItemWidth="160" ItemHeight="160"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="Margin" Value="8"/>
                    </Style>
                </GridView.ItemContainerStyle>
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="140" Height="160" Spacing="6">
                            <StackPanel.ContextFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Start" Click="StartMenu_Click" Visibility="{Binding IsAvailable, Converter={StaticResource BoolToVisibility}}"/>
                                    <MenuFlyoutItem Text="Add Items" Click="AddItemsMenu_Click" Visibility="{Binding Occupied, Converter={StaticResource BoolToVisibility}}"/>
                                    <MenuFlyoutItem Text="Stop" Click="StopMenu_Click" Visibility="{Binding Occupied, Converter={StaticResource BoolToVisibility}}"/>
                                </MenuFlyout>
                            </StackPanel.ContextFlyout>
                            <Grid Width="120" Height="120">
                                <Ellipse Fill="{Binding BackgroundBrush}"/>
                                <Ellipse Width="110" Height="110" Fill="#777" Opacity="0.35"/>
                                <Ellipse Width="100" Height="100" Fill="#aaa" Opacity="0.25"/>
                            </Grid>
                            <TextBlock Text="{Binding Label}" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding StatusText}" HorizontalAlignment="Center" Opacity="0.8"/>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </ScrollViewer>
</Page>
