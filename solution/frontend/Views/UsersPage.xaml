<Page
    x:Class="MagiDesk.Frontend.Views.UsersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12" RowSpacing="8">
        <!-- CRITICAL FIX: Replace CommunityToolkit DataGrid with native WinUI 3 ListView -->
        <!-- CommunityToolkit DataGrid causes COM exceptions in WinUI 3 Desktop Apps -->
        <ListView x:Name="Grid"
                  ItemsSource="{Binding Users}"
                  SelectionMode="Single"
                  RightTapped="Grid_RightTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Username -->
                        <TextBlock Grid.Column="0" Text="{Binding Username}" VerticalAlignment="Center"/>
                        
                        <!-- Role -->
                        <TextBlock Grid.Column="1" Text="{Binding Role}" VerticalAlignment="Center"/>
                        
                        <!-- Created -->
                        <TextBlock Grid.Column="2" Text="{Binding CreatedAt}" VerticalAlignment="Center"/>
                        
                        <!-- Updated -->
                        <TextBlock Grid.Column="3" Text="{Binding UpdatedAt}" VerticalAlignment="Center"/>
                        
                        <!-- Active -->
                        <CheckBox Grid.Column="4" IsChecked="{Binding IsActive}" IsEnabled="False" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Refresh" Icon="Refresh" Click="Refresh_Click" />
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Text="New" Icon="Add" Click="New_Click" />
                    <MenuFlyoutItem Text="Edit" Icon="Edit" Click="Edit_Click" />
                    <MenuFlyoutItem Text="Toggle Active" Icon="Accept" Click="Deactivate_Click" />
                    <MenuFlyoutItem Text="Delete" Icon="Delete" Click="Delete_Click" />
                </MenuFlyout>
            </ListView.ContextFlyout>
        </ListView>

        <InfoBar x:Name="Info" IsOpen="False" Severity="Success"/>
    </Grid>
</Page>
