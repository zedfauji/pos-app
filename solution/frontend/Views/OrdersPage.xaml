<Page
    x:Class="MagiDesk.Frontend.Views.OrdersPage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">
    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="Order" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="ID:"/>
            <TextBlock Text="{Binding OrderId}"/>
            <Button Content="Refresh" Click="Refresh_Click"/>
            <Button Content="Load Logs" Click="LoadLogs_Click"/>
            <Button Content="Checkout" Click="Checkout_Click"/>
            <NumberBox x:Name="OrderIdBox" Width="140" Minimum="1" SmallChange="1" PlaceholderText="Enter OrderId"/>
            <Button Content="Load" Click="LoadOrderId_Click"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- CRITICAL FIX: Replace CommunityToolkit DataGrid with native WinUI 3 ListView -->
            <!-- CommunityToolkit DataGrid causes COM exceptions in WinUI 3 Desktop Apps -->
            <ListView Grid.Column="0"
                      x:Name="ItemsGrid"
                      ItemsSource="{Binding Items}"
                      SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Item Name -->
                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"/>
                            
                            <!-- Modifiers -->
                            <TextBlock Grid.Column="1" Text="{Binding ModifiersText}" VerticalAlignment="Center"/>
                            
                            <!-- Quantity Controls -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                                <Button Content="-" Click="QtyMinus_Click" Width="24" Height="24"/>
                                <TextBlock Text="{Binding Quantity}" Width="32" TextAlignment="Center" VerticalAlignment="Center"/>
                                <Button Content="+" Click="QtyPlus_Click" Width="24" Height="24"/>
                            </StackPanel>
                            
                            <!-- Price -->
                            <TextBlock Grid.Column="3" Text="{Binding UnitPrice}" VerticalAlignment="Center"/>
                            
                            <!-- Total -->
                            <TextBlock Grid.Column="4" Text="{Binding LineTotal}" VerticalAlignment="Center"/>
                            
                            <!-- Actions -->
                            <Button Grid.Column="5" Content="Cancel" Click="Cancel_Click" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock Text="Audit Logs" FontWeight="SemiBold"/>
                <ListView ItemsSource="{Binding Logs}" Height="260">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Action}"/>
                                <TextBlock Text="{Binding CreatedAt}" FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="16" HorizontalAlignment="Right">
            <TextBlock Text="{Binding Subtotal}"/>
            <TextBlock Text="{Binding DiscountTotal}"/>
            <TextBlock Text="{Binding TaxTotal}"/>
            <TextBlock FontWeight="Bold" Text="{Binding Total}"/>
            <TextBlock Foreground="ForestGreen" Text="{Binding ProfitMargin}"/>
        </StackPanel>
    </Grid>
</Page>
