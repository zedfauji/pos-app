<Page
    x:Class="MagiDesk.Frontend.Views.SessionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
            <TextBlock Text="Sessions" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="(auto-refreshing)" Opacity="0.6"/>
            <ToggleSwitch x:Name="ActiveOnlyToggle" Header="Show active only" Toggled="ActiveOnlyToggle_Toggled"/>
            <ComboBox x:Name="StatusFilterCombo" Width="160" SelectionChanged="StatusFilterCombo_SelectionChanged">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Active"/>
                <ComboBoxItem Content="Closed"/>
            </ComboBox>
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="From:" VerticalAlignment="Center"/>
                <DatePicker x:Name="FromDatePicker" DateChanged="FromDatePicker_DateChanged"/>
                <TextBlock Text="To:" VerticalAlignment="Center"/>
                <DatePicker x:Name="ToDatePicker" DateChanged="ToDatePicker_DateChanged"/>
                <TextBox x:Name="TableFilterText" PlaceholderText="Table filter" Width="140" TextChanged="TextFilter_Changed"/>
                <TextBox x:Name="ServerFilterText" PlaceholderText="Server filter" Width="140" TextChanged="TextFilter_Changed"/>
                <Button x:Name="RefreshNowButton" Content="Refresh Now" Click="RefreshNowButton_Click"/>
            </StackPanel>
        </StackPanel>
        <!-- CRITICAL FIX: Replace CommunityToolkit DataGrid with native WinUI 3 ListView -->
        <!-- CommunityToolkit DataGrid causes COM exceptions in WinUI 3 Desktop Apps -->
        <ListView Grid.Row="1"
                  x:Name="SessionsGrid"
                  ItemsSource="{x:Bind Sessions, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Session ID -->
                        <TextBlock Grid.Column="0" Text="{Binding SessionId}" VerticalAlignment="Center"/>
                        
                        <!-- Billing ID -->
                        <TextBlock Grid.Column="1" Text="{Binding BillingId}" VerticalAlignment="Center"/>
                        
                        <!-- Table -->
                        <TextBlock Grid.Column="2" Text="{Binding TableId}" VerticalAlignment="Center"/>
                        
                        <!-- Server -->
                        <TextBlock Grid.Column="3" Text="{Binding ServerName}" VerticalAlignment="Center"/>
                        
                        <!-- Start Time -->
                        <TextBlock Grid.Column="4" Text="{Binding StartTime}" VerticalAlignment="Center"/>
                        
                        <!-- Status -->
                        <TextBlock Grid.Column="5" Text="{Binding Status}" VerticalAlignment="Center"/>
                        
                        <!-- Items Count -->
                        <TextBlock Grid.Column="6" Text="{Binding ItemsCount}" VerticalAlignment="Center"/>
                        
                        <!-- Total -->
                        <TextBlock Grid.Column="7" Text="{Binding Total}" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
