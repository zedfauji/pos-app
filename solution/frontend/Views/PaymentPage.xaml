<Page
    x:Class="MagiDesk.Frontend.Views.PaymentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Refresh Button -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Unsettled Payments" FontSize="28" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button x:Name="RefreshButton" Content="Refresh" Margin="20,0,0,0" Click="RefreshButton_Click"/>
        </StackPanel>

        <!-- Statistics Panel -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Background="LightBlue" Padding="15" Margin="0,0,10,0" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Total Unsettled" FontSize="12" Foreground="Gray"/>
                    <TextBlock Text="{x:Bind ViewModel.TotalUnsettledAmount, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Background="LightGreen" Padding="15" Margin="5,0" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Pending Bills" FontSize="12" Foreground="Gray"/>
                    <TextBlock Text="{x:Bind ViewModel.UnsettledBills.Count, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="2" Background="LightCoral" Padding="15" Margin="10,0,0,0" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Overdue" FontSize="12" Foreground="Gray"/>
                    <TextBlock Text="{x:Bind ViewModel.OverdueCount, Mode=OneWay}" FontSize="20" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Bills List -->
        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.UnsettledBills, Mode=OneWay}" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="15" BorderBrush="LightGray" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding BillId}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{Binding TableLabel}" Foreground="Gray" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding StartTime}" Foreground="Gray" FontSize="12" Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,10,0">
                            <TextBlock Text="{Binding TotalAmount}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{Binding ServerName}" Foreground="Gray" FontSize="12" Margin="0,2,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button Content="Process" Margin="0,0,5,0" Click="ProcessPayment_Click" Tag="{Binding}"/>
                            <Button Content="Details" Click="ViewDetails_Click" Tag="{Binding}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressRing IsActive="True" Width="50" Height="50"/>
                <TextBlock Text="Loading..." Margin="0,10,0,0" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Error Overlay -->
        <Grid x:Name="ErrorOverlay" Grid.RowSpan="3" Background="#80FF0000" Visibility="{x:Bind ViewModel.HasError, Mode=OneWay}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Error Loading Data" FontSize="18" Foreground="White" FontWeight="SemiBold"/>
                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Margin="0,5,0,0" Foreground="White" TextWrapping="Wrap"/>
                <Button Content="Retry" Margin="0,10,0,0" Click="RefreshButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
