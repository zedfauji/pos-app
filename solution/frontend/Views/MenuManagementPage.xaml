<Page
    x:Class="MagiDesk.Frontend.Views.MenuManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Menu Management" FontSize="28" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="Manage menu items and their modifiers" FontSize="14" Opacity="0.7"/>
        </StackPanel>

        <!-- TabView for Menu Items and Modifiers -->
        <TabView Grid.Row="1" x:Name="MainTabView">
            <!-- Menu Items Tab -->
            <TabViewItem Header="Menu Items">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <!-- Menu Items List -->
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Toolbar -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                            <Button Content="Add Menu Item" 
                                    Click="AddMenuItem_Click"
                                    Style="{StaticResource AccentButtonStyle}"/>
                            <Button Content="Edit" 
                                    Click="EditMenuItem_Click"
                                    x:Name="EditButton"/>
                            <Button Content="Delete" 
                                    Click="DeleteMenuItem_Click"
                                    x:Name="DeleteButton"/>
                            <Button Content="Refresh" 
                                    Click="RefreshMenuItems_Click"/>
                            <TextBlock Text="{Binding MenuItems.Count}" 
                                       FontWeight="Bold" 
                                       Foreground="Red"
                                       VerticalAlignment="Center"
                                       Margin="20,0,0,0"/>
                        </StackPanel>

                        <!-- Menu Items Grid -->
                        <GridView Grid.Row="1"
                                  ItemsSource="{Binding MenuItems}"
                                  SelectionMode="Single"
                                  SelectionChanged="GridView_SelectionChanged">
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Width="200" Height="120">
                                        <Border CornerRadius="8" 
                                                Padding="8" 
                                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                                            <StackPanel Spacing="4">
                                                <Border Height="60" 
                                                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                                                        CornerRadius="4">
                                                    <TextBlock Text="📷" 
                                                               FontSize="24"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                </Border>
                                                <TextBlock Text="{Binding Name}" 
                                                           TextWrapping="Wrap" 
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Price}" 
                                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                                <TextBlock Text="{Binding Sku}" 
                                                           FontSize="12" 
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </Grid>

                    <!-- Separator -->
                    <Border Grid.Column="1" 
                            Background="{ThemeResource ControlStrokeColorDefaultBrush}"/>

                    <!-- Details Panel -->
                    <Grid Grid.Column="2" 
                          x:Name="MenuItemDetailsPanel"
                          Visibility="Collapsed">
                        <ScrollViewer>
                            <StackPanel Spacing="12">
                                <TextBlock Text="Menu Item Details" 
                                           FontSize="18" 
                                           FontWeight="SemiBold"/>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="ID:" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding SelectedMenuItem.MenuItemId}"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Name:" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding SelectedMenuItem.Name}"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Price:" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding SelectedMenuItem.Price}"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="SKU:" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding SelectedMenuItem.Sku}"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Picture URL:" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding SelectedMenuItem.PictureUrl}" 
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Available:" FontWeight="SemiBold"/>
                                    <CheckBox IsChecked="{Binding SelectedMenuItem.IsAvailable}" IsEnabled="False"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Discountable:" FontWeight="SemiBold"/>
                                    <CheckBox IsChecked="{Binding SelectedMenuItem.IsDiscountable}" IsEnabled="False"/>
                                </StackPanel>
                                
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Part of Combo:" FontWeight="SemiBold"/>
                                    <CheckBox IsChecked="{Binding SelectedMenuItem.IsPartOfCombo}" IsEnabled="False"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </TabViewItem>

            <!-- Modifiers Tab -->
            <TabViewItem Header="Modifiers">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Modifiers List -->
                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="Modifiers" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Content="Add Modifier" 
                                    Click="AddModifier_Click" 
                                    Margin="16,0,0,0"/>
                        </StackPanel>

                        <ListView x:Name="ModifiersListView" 
                                  ItemsSource="{Binding Modifiers}"
                                  SelectionChanged="ModifiersListView_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="16" Margin="0,0,0,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16"/>
                                            <TextBlock Text="{Binding Description}" FontSize="12" Opacity="0.7" Margin="0,2,0,0"/>
                                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                                <TextBlock Text="{Binding OptionsCount}" FontSize="10" Opacity="0.5"/>
                                                <TextBlock Text="{Binding IsRequiredText}" FontSize="10" Opacity="0.5"/>
                                                <TextBlock Text="{Binding AllowMultipleText}" FontSize="10" Opacity="0.5"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <Button Grid.Column="1" 
                                                Content="Edit" 
                                                Click="EditModifier_Click" 
                                                Margin="8,0,0,0"/>
                                        <Button Grid.Column="2" 
                                                Content="Delete" 
                                                Click="DeleteModifier_Click" 
                                                Margin="8,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                    <!-- Modifier Details -->
                    <StackPanel Grid.Column="1" x:Name="ModifierDetailsPanel" Visibility="Collapsed">
                        <TextBlock Text="Modifier Details" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>
                        
                        <StackPanel Spacing="12">
                            <StackPanel>
                                <TextBlock Text="Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox x:Name="ModifierNameTextBox"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox x:Name="ModifierDescriptionTextBox" 
                                         AcceptsReturn="True"
                                         Height="60"/>
                            </StackPanel>

                            <CheckBox x:Name="IsRequiredCheckBox" 
                                      Content="Required"/>

                            <CheckBox x:Name="AllowMultipleCheckBox" 
                                      Content="Allow Multiple Selections"/>

                            <StackPanel x:Name="MaxSelectionsPanel" Visibility="Collapsed">
                                <TextBlock Text="Max Selections" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <NumberBox x:Name="MaxSelectionsNumberBox" 
                                           Minimum="1"
                                           Maximum="10"/>
                            </StackPanel>

                            <StackPanel Margin="0,16,0,0">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="Options" FontWeight="SemiBold"/>
                                    <Button Content="Add Option" 
                                            Click="AddOption_Click" 
                                            Margin="16,0,0,0"/>
                                </StackPanel>

                                <ListView x:Name="OptionsListView" 
                                          Height="200">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="8" Margin="0,0,0,4" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{Binding PriceDelta}" FontSize="10" Opacity="0.7"/>
                                                </StackPanel>

                                                <Button Grid.Column="1" 
                                                        Content="Edit" 
                                                        Click="EditOption_Click" 
                                                        Margin="4,0,0,0"/>
                                                <Button Grid.Column="2" 
                                                        Content="Delete" 
                                                        Click="DeleteOption_Click" 
                                                        Margin="4,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                <Button Content="Save" 
                                        Click="SaveModifier_Click" 
                                        Margin="0,0,8,0"/>
                                <Button Content="Cancel" 
                                        Click="CancelEdit_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </TabViewItem>
        </TabView>
    </Grid>
</Page>