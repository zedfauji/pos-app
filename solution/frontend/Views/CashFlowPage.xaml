<Page
    x:Class="MagiDesk.Frontend.Views.CashFlowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Spacing="8">
            <TextBlock x:Name="Title" Text="Cash Flow" FontSize="20" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox x:Name="EmployeeNameBox" Header="Employee Name" Width="220" IsReadOnly="True"/>
                <DatePicker x:Name="EntryDatePicker" Header="Date" Width="180"/>
                <NumberBox x:Name="CashAmountBox" Header="Cash Amount" Width="160"/>
                <Button x:Name="SubmitBtn" Content="Submit Cash Entry" Click="Submit_Click"/>
            </StackPanel>
            <TextBox x:Name="NotesBox" Header="Notes" AcceptsReturn="True" Height="120" TextWrapping="Wrap"/>
            <InfoBar x:Name="FeedbackBar" IsOpen="False"/>
        </StackPanel>

        <CommandBar Grid.Row="1">
            <AppBarButton x:Name="BtnRefresh" Icon="Refresh" Label="Refresh" Click="Refresh_Click"/>
        </CommandBar>

        <!-- CRITICAL FIX: Replace CommunityToolkit DataGrid with native WinUI 3 ListView -->
        <!-- CommunityToolkit DataGrid causes COM exceptions in WinUI 3 Desktop Apps -->
        <ListView Grid.Row="2"
                  x:Name="HistoryGrid"
                  ItemsSource="{Binding HistoryDisplay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Employee -->
                        <TextBlock Grid.Column="0" Text="{Binding EmployeeName}" VerticalAlignment="Center"/>
                        
                        <!-- Date -->
                        <TextBlock Grid.Column="1" Text="{Binding Date}" VerticalAlignment="Center"/>
                        
                        <!-- Cash -->
                        <TextBlock Grid.Column="2" Text="{Binding CashAmount}" VerticalAlignment="Center"/>
                        
                        <!-- Notes -->
                        <TextBlock Grid.Column="3" Text="{Binding Notes}" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
