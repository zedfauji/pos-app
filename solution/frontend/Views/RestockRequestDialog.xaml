<ContentDialog
    x:Class="MagiDesk.Frontend.Views.RestockRequestDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:services="using:MagiDesk.Frontend.Services"
    mc:Ignorable="d"
    Title="Create Restock Request"
    PrimaryButtonText="Create Request"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid MinWidth="500" MinHeight="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Item Selection -->
        <TextBlock Grid.Row="0" Text="Item" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="1" x:Name="ItemComboBox" PlaceholderText="Select item to restock" SelectionChanged="ItemComboBox_SelectionChanged" Margin="0,0,0,8"/>

        <!-- Current Stock Info -->
        <TextBlock Grid.Row="2" Text="Current Stock" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="3" x:Name="CurrentStockText" Text="0" Margin="0,0,0,8"/>

        <!-- Requested Quantity -->
        <TextBlock Grid.Row="4" Text="Requested Quantity" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <NumberBox Grid.Row="5" x:Name="QuantityBox" Minimum="1" Maximum="10000" Value="1" Margin="0,0,0,8"/>

        <!-- Vendor Selection -->
        <TextBlock Grid.Row="6" Text="Preferred Vendor" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="7" x:Name="VendorComboBox" PlaceholderText="Select vendor" Margin="0,0,0,8"/>

        <!-- Unit Cost -->
        <TextBlock Grid.Row="8" Text="Unit Cost" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <NumberBox Grid.Row="9" x:Name="UnitCostBox" Minimum="0" Maximum="10000" Value="0" Margin="0,0,0,8"/>

        <!-- Notes -->
        <TextBlock Grid.Row="10" Text="Notes (Optional)" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="11" x:Name="NotesBox" PlaceholderText="Add any notes about this restock request..." AcceptsReturn="True" TextWrapping="Wrap" Height="80"/>
    </Grid>
</ContentDialog>



