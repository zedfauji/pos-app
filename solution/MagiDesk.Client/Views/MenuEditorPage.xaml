<Page
    x:Class="MagiDesk.Client.Views.MenuEditorPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MagiDesk.Client.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Spacing="24" Grid.Row="0">
            <TextBlock Text="Menu Management" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <AppBarButton Icon="Add" Label="Add Item" Command="{x:Bind ViewModel.AddItemCommand}" />
            <AppBarButton Icon="Refresh" Label="Refresh" Command="{x:Bind ViewModel.LoadItemsCommand}" />
        </StackPanel>

        <!-- Loading -->
        <ProgressBar IsIndeterminate="True" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Grid.Row="1" Margin="0,12" />
        <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Foreground="Red" Grid.Row="1" Margin="0,12" Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}"/>
        
        <!-- List -->
        <ListView Grid.Row="2" 
                  ItemsSource="{x:Bind ViewModel.MenuItems, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border CornerRadius="4" Width="48" Height="48" VerticalAlignment="Center">
                            <Image Source="{Binding PictureUrl}" Stretch="UniformToFill"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                            <TextBlock Text="{Binding Name}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock Text="{Binding Sku}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6"/>
                            <TextBlock Text="{Binding Category}" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.8"/>
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{Binding SellingPrice, Converter={StaticResource CurrencyConverter}}" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,24,0"/>

                        <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                             <Button Content="Edit" Command="{Binding ElementName=RootPage, Path=ViewModel.EditItemCommand}" CommandParameter="{Binding}" />
                             <Button Content="Delete" Command="{Binding ElementName=RootPage, Path=ViewModel.DeleteItemCommand}" CommandParameter="{Binding}" Background="DarkRed" Foreground="White"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
