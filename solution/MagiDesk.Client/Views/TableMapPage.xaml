<UserControl
    x:Class="MagiDesk.Client.Views.TableMapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MagiDesk.Client.ViewModels"
    xmlns:dto="using:MagiDesk.Shared.DTOs.Tables"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="10" Margin="10">
            <TextBlock Text="Table Map" Style="{StaticResource TitleTextBlockStyle}"/>
            <Button Content="Refresh" Command="{Binding LoadTablesCommand}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Center"/>
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tables}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapGrid Orientation="Horizontal" MaximumRowsOrColumns="5"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="dto:TableStatusDto">
                    <Button Width="160" Height="110" Margin="10" Padding="0"
                            HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                            Command="{Binding DataContext.SelectTableCommand, ElementName=RootGrid}"
                            CommandParameter="{x:Bind}">
                        <Button.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Move Table" 
                                                Command="{Binding DataContext.MoveTableCommand, ElementName=RootGrid}"
                                                CommandParameter="{x:Bind}"/>
                            </MenuFlyout>
                        </Button.ContextFlyout>
                        <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                              CornerRadius="8" BorderBrush="Gray" BorderThickness="1">
                             <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                 <TextBlock Text="{x:Bind Label}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                 <TextBlock Text="{x:Bind Type}" Foreground="Gray"/>
                                 <TextBlock Text="Occupied" Foreground="Green" Visibility="{x:Bind Occupied}"/>
                             </StackPanel>
                        </Grid>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
