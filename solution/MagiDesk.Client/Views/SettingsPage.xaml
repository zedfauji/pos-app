<Page
    x:Class="MagiDesk.Client.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MagiDesk.Client.ViewModels"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" Margin="24,24,0,24"/>

        <ScrollViewer Grid.Row="1" Padding="24,0,24,24">
            <StackPanel Spacing="20" MaxWidth="600" HorizontalAlignment="Left">
                
                <TextBlock Text="Receipt Customization" Style="{StaticResource SubtitleTextBlockStyle}"/>
                
                <TextBox Header="Restaurant Name" Text="{x:Bind ViewModel.RestaurantName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Header="Address / Phone" Text="{x:Bind ViewModel.RestaurantAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Header="Receipt Footer Message" Text="{x:Bind ViewModel.ReceiptFooter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                
                <TextBlock Text="Financial" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,20,0,0"/>

                <NumberBox Header="Tax Rate (%)" Value="{x:Bind ViewModel.TaxRate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SpinButtonPlacementMode="Inline" />

                <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,20,0,0">
                    <Button Content="Save Settings" Command="{x:Bind ViewModel.SaveCommand}" Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>

                <TextBlock Text="Operations (Admin)" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,30,0,0"/>
                
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}" BorderThickness="1" CornerRadius="4" Padding="16">
                    <StackPanel Spacing="10">
                        <TextBlock Text="End of Day" FontWeight="SemiBold"/>
                        <TextBlock Text="Print the Z-Report and close the operational day. Using this will generate an aggregated report of all sales." 
                                   Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap"/>
                        
                        <Button Content="Print Z-Report" 
                                Command="{x:Bind ViewModel.DayCloseViewModel.PrintZReportCommand}" 
                                IsEnabled="{x:Bind ViewModel.DayCloseViewModel.IsLoading, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"/>
                        
                        <TextBlock Text="{x:Bind ViewModel.DayCloseViewModel.StatusMessage, Mode=OneWay}" 
                                   Foreground="{ThemeResource SystemControlForegroundAccentBrush}" 
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
