<ContentDialog
    x:Class="MagiDesk.Frontend.Views.PurchaseOrderDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:services="using:MagiDesk.Frontend.Services"
    mc:Ignorable="d"
    Title="Create Purchase Order"
    PrimaryButtonText="Create Order"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid MinWidth="600" MinHeight="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Vendor Selection -->
        <TextBlock Grid.Row="0" Text="Vendor" Style="{StaticResource SubtitleTextStyle}" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="1" x:Name="VendorComboBox" PlaceholderText="Select vendor" Margin="0,0,0,8"/>

        <!-- Priority -->
        <TextBlock Grid.Row="2" Text="Priority" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="3" x:Name="PriorityComboBox" PlaceholderText="Select priority" Margin="0,0,0,8">
            <ComboBoxItem Content="Low" Tag="Low"/>
            <ComboBoxItem Content="Medium" Tag="Medium"/>
            <ComboBoxItem Content="High" Tag="High"/>
            <ComboBoxItem Content="Urgent" Tag="Urgent"/>
        </ComboBox>

        <!-- Expected Delivery -->
        <TextBlock Grid.Row="4" Text="Expected Delivery Date" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <DatePicker Grid.Row="5" x:Name="DeliveryDatePicker" Margin="0,0,0,8"/>

        <!-- Notes -->
        <TextBlock Grid.Row="6" Text="Notes" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="7" x:Name="NotesBox" PlaceholderText="Add any notes about this order..." AcceptsReturn="True" TextWrapping="Wrap" Height="100"/>

        <!-- Order Items Placeholder -->
        <TextBlock Grid.Row="8" Text="Order Items will be managed here" FontStyle="Italic" Opacity="0.7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</ContentDialog>



